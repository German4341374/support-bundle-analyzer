extends: default
ignore: |
  deploy/helm/*/templates
rules:
  document-start: disable
  line-length:
    max: 180
    level: warning
  comments:
    min-spaces-from-content: 1
  truthy:
    allowed-values: ["true", "false", "on"]
