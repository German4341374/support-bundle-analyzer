# ADR 0008: Generate a network-free static report

Status: Accepted

The report bundles escaped data, CSS, and plain JavaScript in one directory and does not load CDN assets. This supports offline sharing and review while keeping raw artifacts excluded by default.

