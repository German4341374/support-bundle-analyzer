# Synthetic fixtures

Fixtures in this repository contain only fictional hostnames, reserved documentation IP ranges, and placeholder credentials. The deterministic database-outage archive is generated by `support-bundle-analyzer generate-demo`; archive-security tests create traversal, link, duplicate-path, and compression-ratio cases at test time so unsafe files are never distributed as reusable archives.
